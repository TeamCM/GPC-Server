hierarchy of directions is

> packages/PLATFORM/PACKAGE
  \> latestVersion
  -> VERSION (dir)
     -> PACKAGE_VERSION_PLATFORM
     -> dependencies

WARNING PACKAGE, VERSION or PLATFORM cannot have _ in their values, otherwise unexpected behavior will happen